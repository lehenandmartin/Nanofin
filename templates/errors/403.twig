{% extends 'layouts/base.twig' %}

{% block title %}403 — {{ trans('error.forbidden') }}{% endblock %}

{% block content %}
<div class="max-w-lg mx-auto px-4 py-24 text-center">
    <p class="text-6xl font-black text-orange-500 mb-4">403</p>
    <h1 class="text-2xl font-bold mb-2">{{ trans('error.forbidden') }}</h1>
    <p class="text-[var(--color-muted)] mb-8">{{ trans('error.forbidden_detail') }}</p>
    <a href="{{ url('/') }}" class="btn-primary">{{ trans('error.back_home') }}</a>
</div>
{% endblock %}
