# Deny direct HTTP access to the data directory.
# This directory is outside the webroot so Apache should never serve it,
# but this file adds an extra layer of defense-in-depth.
<IfVersion >= 2.4>
    Require all denied
</IfVersion>
<IfVersion < 2.4>
    Order deny,allow
    Deny from all
</IfVersion>
